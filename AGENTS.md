# Default Working Rules

- Scope: Work only inside this project folder by default.
- Outside this project folder: read-only by default.
- If write access or command execution outside this project is needed, ask the user first.
